# ClassTrack
ClassTrack is a Java-based console Student Attendance Management System using JDBC and MySQL. It implements role-based access (HOD, Faculty, Student), supports secure login, attendance marking (single &amp; batch), percentage calculation, and detailed logging. Built with modular architecture, validation, and schema migration for reliability.
